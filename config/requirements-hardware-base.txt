# Base Hardware Requirements
# ------------------------
# This file contains dependencies for systems without specialized hardware acceleration.
# It includes the standard PyTorch installation without specific optimizations.

# Core Dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# System Dependencies
numpy==1.26.4  # Pin numpy for compatibility
pillow>=9.3.0
requests>=2.28.0
flask>=2.2.0

# Deep Learning Utilities
transformers>=4.30.0
diffusers>=0.27.0
accelerate>=0.23.0
safetensors>=0.3.1

# These are base requirements that will work on all hardware
# For hardware-specific optimizations, see the corresponding hardware-specific requirements files 